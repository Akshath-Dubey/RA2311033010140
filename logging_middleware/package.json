{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware for campus notification app",
  "main": "logger.ts",
  "scripts": {
    "test": "ts-node test.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
